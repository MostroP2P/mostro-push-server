---
phase: 02-post-api-notify-endpoint-with-privacy-hardening
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/verification/dispute-chat.md
autonomous: true
requirements:
  - VERIFY-03
commit_message_prefix: "docs: add dispute chat verification runbook"
tags:
  - docs
  - runbook
  - verification
  - dispute-chat
  - anti-crit-1

must_haves:
  truths:
    - "docs/verification/dispute-chat.md exists and walks an operator through dispute-chat verification end-to-end."
    - "Runbook is in Spanish per global CLAUDE.md (runbook is operator-facing project documentation; project default is English but global CLAUDE.md applies for runbook tone)."
    - "Runbook contains the explicit anti-CRIT-1 reminder: no .authors(mostro_pubkey) filter must ever be added to the Nostr listener filter chain (admin DMs are direct user-to-user, NOT routed through the Mostro daemon)."
    - "Runbook includes the bash grep one-liner that an operator can run to confirm the anti-CRIT-1 anti-fix has not been re-introduced (! grep -nE '\\.authors\\(' src/nostr/listener.rs)."
    - "Runbook documents the four steps from D-17: register test pubkey, publish kind 1059 from second Nostr client (NOT Mostro daemon), verify info!(\"Push sent successfully for event ...\") + device receives push, run anti-CRIT-1 grep check."
  artifacts:
    - path: docs/verification/dispute-chat.md
      provides: "Operator runbook for dispute-chat verification path; reinforces anti-CRIT-1 anti-fix"
      contains: "kind 1059"
      min_lines: 50
  key_links:
    - from: docs/verification/dispute-chat.md
      to: src/nostr/listener.rs
      via: "grep one-liner that confirms no .authors(...) filter was added"
      pattern: "grep .* \\.authors\\("
    - from: docs/verification/dispute-chat.md
      to: POST /api/register
      via: "step 1 of the runbook — register the test pubkey before publishing the kind 1059 event"
      pattern: "POST /api/register"
---

<objective>
Ship the operator-facing manual runbook for dispute-chat verification at `docs/verification/dispute-chat.md`. This is the standalone third commit in Phase 2 per `02-CONTEXT.md` D-19 and is the sole deliverable for VERIFY-03. The runbook documents how an operator confirms — after Phase 2 deploys to staging — that admin DMs (sent directly user-to-user, NOT routed through the Mostro daemon) still reach a registered device as a silent push via the existing Nostr-listener path that Phase 1 refactored and Phase 2 left untouched.

Purpose: Implement D-17 (`docs/verification/dispute-chat.md` is the single deliverable for VERIFY-03) and D-18 (no test code for the dispute-chat path in Phase 2). Reinforce the anti-CRIT-1 anti-fix at the runbook level so future contributors that look at the dormant `MOSTRO_PUBKEY` validation see the explicit reason the listener filter cannot ever gain a `.authors(mostro_pubkey)` chain.

Output: One file created (`docs/verification/dispute-chat.md`), in Spanish, professional in tone, no emojis (per global CLAUDE.md). One atomic commit `docs: add dispute chat verification runbook`.
</objective>

<context_summary>
Locked decisions from `02-CONTEXT.md`:
- **D-17**: `docs/verification/dispute-chat.md` is the single deliverable. Four sections — register test pubkey, publish kind 1059 from a second Nostr client, verify info!() log + device push, anti-CRIT-1 grep check. Written in Spanish.
- **D-18**: No test code in Phase 2 for the dispute-chat path. Manual runbook only.

Project constraints (global + project CLAUDE.md):
- Conversation in Spanish; runbook content in Spanish.
- Code, comments, commit messages, branch names: English. Runbook content (Spanish prose) intentionally diverges from this rule per global CLAUDE.md "runbook is operator-facing".
- No emojis in documentation.
- Professional tone.

This plan does NOT depend on Plan 02 — the runbook documents the EXISTING (unchanged) Nostr-listener path. It depends only on Plan 01 (the constructor cascade) for byte-identity of build artifacts. In practice, Plans 02 and 03 can run in parallel within Wave 2 (no code overlap, no shared file).

Anti-requirements reinforced by this runbook:
- OOS-19 / CRIT-1: No `.authors(mostro_pubkey)` filter on the Nostr listener.
- OOS-10: No auth on the `/api/notify` endpoint (mentioned tangentially — runbook does NOT exercise `/api/notify` because that is Plan 02's smoke; this runbook exercises the listener-pulled-events path).
</context_summary>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/02-post-api-notify-endpoint-with-privacy-hardening/02-CONTEXT.md
@.planning/phases/02-post-api-notify-endpoint-with-privacy-hardening/02-RESEARCH.md
@.planning/phases/02-post-api-notify-endpoint-with-privacy-hardening/02-PATTERNS.md
@.planning/phases/01-pushdispatcher-refactor-no-behaviour-change/01-01-SUMMARY.md
@src/nostr/listener.rs

<interfaces>
<!-- Anti-CRIT-1 grep skeleton from RESEARCH.md (Code Examples section, lines 891-907). -->
<!-- The runbook reproduces this verbatim in the verification step. -->

```bash
# Verify no .authors(...) filter has crept into the listener.
# Expected output: a single match — the comment block from Phase 1's D-11 — and
# nothing else. Matches inside src/nostr/listener.rs that are NOT comments fail
# the anti-requirement.
grep -n '\.authors(' src/nostr/listener.rs

# Bash exit-status check the runbook can use:
if grep -nE '^\s*[^/].*\.authors\(' src/nostr/listener.rs; then
    echo "FAIL: .authors() filter present in listener — anti-CRIT-1 violated"
    exit 1
else
    echo "PASS: no active .authors() filter"
fi
```

The expected output of `grep -n '\.authors(' src/nostr/listener.rs` after Phase 1
shipped is exactly ONE match: the comment block above `Filter::new()` that
explicitly says "DO NOT add .authors(...)" (Phase 1 D-11).
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> staging server | manual flyctl-mediated commands; the operator is trusted. |
| second Nostr client -> configured relay | publishes a `kind 1059` Gift Wrap event addressed to the test `trade_pubkey`. The relay is trusted within the test fixture; in production this same path carries admin DMs sent directly user-to-user. |
| reviewer reading the runbook -> future contributors | the runbook serves as a soft barrier (English-then-Spanish text) for a future contributor who might "fix" the dormant `MOSTRO_PUBKEY` validation by applying it as a filter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering (anti-CRIT-1 regression) | src/nostr/listener.rs Filter::new() chain | mitigate | Runbook step 4: explicit `grep -nE '\\.authors\\(' src/nostr/listener.rs` check the operator runs after every Phase 2-onward deploy. Expected: 1 match (the Phase 1 anti-CRIT-1 comment block). >1 match = FAIL. The runbook also includes a paragraph of prose explaining WHY the filter cannot exist (admin DMs are direct user-to-user, NIP-59 ephemeral outer keys, OOS-19 / CRIT-1). |
| T-02-RUNBOOK-MISLEADING | Repudiation | future contributor reads the runbook | accept | Runbook is in Spanish per project convention for operator runbooks; English readers may need translation. Acceptable risk: the project's developer audience reads Spanish (per global CLAUDE.md). |
| T-02-RUNBOOK-RELAY-SHARED | Information Disclosure | publishing a kind 1059 to a public relay | accept | The runbook's test event is structurally identical to a real Mostro Gift Wrap (NIP-59) — the outer key is ephemeral, the `p` tag is the test `trade_pubkey`. Publishing this to a public relay is privacy-equivalent to publishing any other Gift Wrap; no extra information is leaked. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/verification/ directory and write dispute-chat.md runbook (Spanish)</name>
  <files>docs/verification/dispute-chat.md</files>
  <read_first>
    - .planning/phases/02-post-api-notify-endpoint-with-privacy-hardening/02-CONTEXT.md (D-17 spec — four sections; D-18 manual-only)
    - .planning/phases/02-post-api-notify-endpoint-with-privacy-hardening/02-RESEARCH.md (Code Examples section "Anti-CRIT-1 grep one-liner for the runbook" lines 891-907)
    - .planning/phases/02-post-api-notify-endpoint-with-privacy-hardening/02-PATTERNS.md (section "CREATE docs/verification/dispute-chat.md")
    - .planning/PROJECT.md (anti-requirement OOS-19 / CRIT-1 wording for the prose section)
    - src/nostr/listener.rs (the anti-CRIT-1 comment block above Filter::new() — Phase 1 landed it; the runbook references the file by path, lines optional)
  </read_first>
  <action>
    Create the directory `docs/verification/` (it does not exist yet — the executor's `Write` tool will create the parent directory implicitly when it writes the file). Write the file `docs/verification/dispute-chat.md` with the following exact Spanish content:

    ````markdown
    # Verificación manual: ruta de chat de disputa

    Este runbook describe cómo un operador verifica de extremo a extremo que
    los DMs administrativos (enviados directamente usuario-a-usuario, NO
    enrutados a través del daemon de Mostro) siguen alcanzando los
    dispositivos registrados como push silenciosos a través de la ruta de
    escucha de Nostr.

    Este procedimiento se ejecuta después de cada despliegue del milestone
    v1.1 (fases 1, 2 y 3). Es la única señal de extremo a extremo de que el
    refactor de la fase 1 (`PushDispatcher`) y la adición de la fase 2
    (`POST /api/notify`) no han introducido una regresión silenciosa en la
    ruta del escucha.

    ## Por qué este runbook existe

    En Mostro, los chats de disputa entre administradores y usuarios se
    envían como mensajes Nostr directos `kind 1059` (Gift Wrap, NIP-59).
    El remitente es el administrador (un usuario humano), NO el daemon de
    Mostro. El daemon de Mostro publica eventos de actualización de trade,
    pero NO los DMs administrativos.

    Por esta razón, el escucha de Nostr en `src/nostr/listener.rs` NO
    DEBE filtrar los eventos por `authors`. Filtrar por
    `mostro_pubkey` haría que los DMs administrativos se descartaran en
    silencio, rompiendo el chat de disputa para todos los usuarios.

    Adicionalmente, Gift Wrap (NIP-59) usa una clave externa efímera por
    evento. La clave del remitente nunca es visible en el evento externo,
    por lo que filtrar por `authors` es estructuralmente imposible
    incluso si se quisiera.

    Esta restricción está documentada como anti-requisito **OOS-19 /
    CRIT-1** en `.planning/PROJECT.md` y como bloque de comentario sobre
    `Filter::new()` en `src/nostr/listener.rs` (introducido en la fase 1
    decisión D-11).

    ## Prerrequisitos

    - Acceso al staging de Fly.io (`mostro-push-server`).
    - `flyctl` instalado y autenticado (`flyctl auth whoami` debe
      responder).
    - Un cliente Nostr secundario capaz de publicar eventos `kind 1059`
      contra un relay configurado por el servidor (por ejemplo, Damus,
      Amethyst, o un script `nostr-tool`).
    - Una `trade_pubkey` de prueba (64 caracteres hex) y un token FCM o
      UnifiedPush de prueba registrado para esa pubkey.
    - Un dispositivo de prueba (real o emulador) capaz de recibir el push
      silencioso correspondiente al token registrado.
    - Acceso de solo lectura al árbol de fuentes del repo
      (`mostro-push-server`) para ejecutar la verificación grep
      anti-CRIT-1 al final del procedimiento.

    ## Procedimiento

    ### Paso 1: Registrar la pubkey de prueba

    Registra el `trade_pubkey` y el token de dispositivo a través del
    endpoint existente `POST /api/register`:

    ```bash
    curl -i -X POST https://mostro-push-server.fly.dev/api/register \
      -H 'content-type: application/json' \
      -d '{
        "trade_pubkey": "<64-hex-chars>",
        "token": "<token-fcm-o-unifiedpush>",
        "platform": "android"
      }'
    ```

    Respuesta esperada: `200 OK` con cuerpo
    `{"success":true,"message":"Token registered successfully","platform":"android"}`.

    Anota el `trade_pubkey` exacto que registraste — lo necesitarás en el
    paso siguiente.

    ### Paso 2: Publicar un evento kind 1059 desde un cliente Nostr secundario

    Desde un cliente Nostr secundario (NO el daemon de Mostro — es
    importante simular que el remitente es un usuario humano, no el
    daemon), publica un Gift Wrap (NIP-59, `kind 1059`) dirigido al
    `trade_pubkey` registrado, contra uno de los relays configurados
    en el servidor (`NOSTR_RELAYS`).

    El evento debe tener:

    - `kind`: `1059`
    - Etiqueta `p`: el `trade_pubkey` registrado en el paso 1.
    - Clave externa: efímera (cualquier `Keys::generate()` u
      equivalente). Esta es la clave que firma el evento externo;
      NO es la clave del administrador.

    El contenido interno del Gift Wrap puede ser cualquier mensaje de
    prueba; el servidor de push no descifra ni inspecciona el contenido.

    El daemon de Mostro NO envía DMs administrativos. Los administradores
    contactan a los usuarios directamente (de usuario a usuario). Filtrar
    por `mostro_pubkey` en el escucha rompería esta ruta silenciosamente,
    razón por la cual la fase 1 D-11 introdujo el bloque de comentario
    explícito en `src/nostr/listener.rs` y la fase 3 OPEN-6 mantiene el
    campo `MOSTRO_PUBKEY` inerte sin aplicarlo como filtro.

    ### Paso 3: Verificar la entrega del push silencioso

    Inmediatamente después de publicar el evento, observa los logs del
    servidor en Fly.io:

    ```bash
    flyctl logs -a mostro-push-server | grep -E "(Push sent successfully for event|Failed to send push)"
    ```

    Debes ver una línea de la forma:

    ```
    Push sent successfully for event <event-id>
    ```

    dentro de los siguientes ~5-10 segundos tras publicar el evento.

    Adicionalmente, el dispositivo de prueba debe recibir el push
    silencioso. Verifícalo según el backend:

    - **FCM (iOS o Android)**: el handler en background del cliente
      móvil debe ejecutarse (por ejemplo, `didReceiveRemoteNotification`
      en iOS, `FirebaseMessagingService.onMessageReceived` en Android).
    - **UnifiedPush (Android)**: el receptor en background del cliente
      debe activarse según el distribuidor configurado.

    Si NO ves la línea `Push sent successfully for event ...` en los
    logs:

    - Confirma que el evento llegó al relay configurado (algunas
      herramientas Nostr fallan en silencio si el relay rechaza el
      evento).
    - Confirma que el `p` tag coincide exactamente con el
      `trade_pubkey` registrado (64 caracteres hex, sin
      mayúsculas/minúsculas mezcladas).
    - Revisa los logs completos para errores en `connect_and_listen` o
      en `dispatcher.dispatch(...)`.

    ### Paso 4: Verificación anti-CRIT-1

    Después de cada despliegue, ejecuta el siguiente comando en el árbol
    de fuentes del repo para confirmar que no se ha re-introducido el
    anti-fix prohibido (`.authors(mostro_pubkey)` en el filtro del
    escucha):

    ```bash
    grep -n '\.authors(' src/nostr/listener.rs
    ```

    Salida esperada: exactamente UNA coincidencia, dentro del bloque de
    comentario de la fase 1 D-11 (la línea que dice
    `// DO NOT add .authors(...) here.` o equivalente).

    Para una verificación con código de salida bash que falle si
    aparece una llamada `.authors(...)` activa (no en comentario):

    ```bash
    if grep -nE '^\s*[^/].*\.authors\(' src/nostr/listener.rs; then
        echo "FAIL: filtro .authors() presente en el escucha — anti-CRIT-1 violado"
        exit 1
    else
        echo "PASS: no hay filtro .authors() activo"
    fi
    ```

    Si el comando de salida falla (`exit 1`), abre un issue de
    seguridad de inmediato — alguien ha re-introducido el anti-fix
    prohibido y los DMs administrativos están siendo descartados en
    silencio.

    ## Limpieza

    Después de verificar, elimina el token de prueba para evitar
    contaminar las métricas de producción:

    ```bash
    curl -i -X POST https://mostro-push-server.fly.dev/api/unregister \
      -H 'content-type: application/json' \
      -d '{"trade_pubkey": "<64-hex-chars>"}'
    ```

    Respuesta esperada: `200 OK` con cuerpo
    `{"success":true,"message":"Token unregistered successfully"}`
    o `{"success":true,"message":"Token not found (may have already been unregistered)"}`.

    ## Frecuencia recomendada

    - Después de cada deploy del milestone v1.1 (fases 1, 2 y 3).
    - Después de cualquier modificación de `src/nostr/listener.rs`,
      `src/push/dispatcher.rs`, o `src/push/mod.rs`.
    - Como verificación periódica mensual en producción.

    ## Referencias

    - `.planning/PROJECT.md` — anti-requisito **OOS-19 / CRIT-1**.
    - `.planning/REQUIREMENTS.md` — requisito **VERIFY-03**.
    - `.planning/phases/01-pushdispatcher-refactor-no-behaviour-change/01-CONTEXT.md` — decisión **D-11** (introducción del bloque de comentario anti-CRIT-1).
    - `src/nostr/listener.rs` — bloque de comentario sobre `Filter::new()`.
    - NIP-59 (Gift Wrap, `kind 1059`) — especificación del esquema de
      evento que envuelve los DMs.
    ````

    Notas sobre la creación del archivo:

    - El directorio `docs/verification/` no existe actualmente en el repo. El
      `Write` tool del executor creará la jerarquía implícitamente al
      escribir el archivo.
    - El runbook es prosa profesional en español, sin emojis (cumple con la
      regla del CLAUDE.md global).
    - No incluyas referencias a archivos que no existan en el repo.
    - No referencies números de línea de `src/nostr/listener.rs` (el bloque
      de comentario puede moverse en futuras refactorizaciones; mejor
      referirse a "el bloque de comentario sobre `Filter::new()`").
    - El bloque de código bash con `if grep -nE ...` usa `^\s*[^/]` para
      excluir las coincidencias dentro de comentarios `//` — esto es load-
      bearing y no debe alterarse.
  </action>
  <verify>
    <automated>test -f docs/verification/dispute-chat.md &amp;&amp; wc -l docs/verification/dispute-chat.md &amp;&amp; grep -cE 'kind 1059' docs/verification/dispute-chat.md &amp;&amp; grep -cE '\.authors\(' docs/verification/dispute-chat.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/verification/dispute-chat.md` (file exists).
    - `wc -l docs/verification/dispute-chat.md` returns at least 50 lines (substantive runbook content).
    - `grep -cE 'kind 1059' docs/verification/dispute-chat.md` returns at least 2 matches (referenced in Por qué section + Paso 2).
    - `grep -cE '\.authors\(' docs/verification/dispute-chat.md` returns at least 2 matches (the grep command + the bash if-grep variant).
    - `grep -cE 'CRIT-1' docs/verification/dispute-chat.md` returns at least 2 matches (anti-requirement reference).
    - `grep -cE 'OOS-19' docs/verification/dispute-chat.md` returns at least 1 match.
    - `grep -nE 'POST /api/register' docs/verification/dispute-chat.md` returns at least 1 match (Paso 1).
    - `grep -nE 'POST /api/unregister' docs/verification/dispute-chat.md` returns at least 1 match (Limpieza).
    - `grep -nE 'flyctl logs' docs/verification/dispute-chat.md` returns at least 1 match (Paso 3).
    - `grep -nE 'Push sent successfully for event' docs/verification/dispute-chat.md` returns at least 1 match (matches the actual listener log line shape from Phase 1 SUMMARY).
    - `grep -nE 'NIP-59' docs/verification/dispute-chat.md` returns at least 1 match.
    - `grep -nE 'usuario-a-usuario|usuario a usuario' docs/verification/dispute-chat.md` returns at least 1 match (Spanish phrasing for "user-to-user").
    - **No emojis check**: `! grep -nE '[\xF0-\xF7]' docs/verification/dispute-chat.md` (no UTF-8 4-byte sequences typical of emojis). NOTE: this regex is approximate; reviewers should also visually scan.
    - **Spanish language anchor**: `grep -cE '(verificación|disputa|administrador|silenciosos)' docs/verification/dispute-chat.md` returns at least 4 matches (Spanish keywords).
    - `! grep -nE '^\s*//' docs/verification/dispute-chat.md` (no Rust-style code comments leaked into prose).
    - **No `/api/notify` step**: this runbook is for the listener path. The endpoint smoke is in Plan 02. Acceptable to mention `/api/notify` exists in passing, but the runbook MUST NOT include `curl POST /api/notify` as a verification step. Verified by `! grep -nE 'curl.*POST.*api/notify' docs/verification/dispute-chat.md`.
  </acceptance_criteria>
  <done>
    `docs/verification/dispute-chat.md` exists with the four-section structure from D-17 (registrar, publicar, verificar, anti-CRIT-1 grep), in Spanish prose, no emojis, includes the bash grep one-liner for anti-CRIT-1 verification, references VERIFY-03 + OOS-19 + CRIT-1 + NIP-59. Word count is substantial (≥ 50 lines, ≥ ~600 words).
  </done>
</task>

</tasks>

<verification>
Plan-level checks (run after the single task completes, BEFORE commit):

1. **File exists**: `test -f docs/verification/dispute-chat.md`.
2. **Scope discipline**: `git diff --name-only HEAD` returns exactly `docs/verification/dispute-chat.md` (this plan creates only one file). If `docs/verification/` did not previously exist, no other file should appear.
3. **Substance check**: `wc -l docs/verification/dispute-chat.md` returns at least 50 lines; `wc -w docs/verification/dispute-chat.md` returns at least 500 words.
4. **Spanish language anchor**: at least 4 of the following Spanish keywords appear: `verificación`, `disputa`, `administrador`, `silencioso`, `escucha`, `daemon`, `relay`, `prerrequisitos`. Verified by `grep -cE '(verificación|disputa|administrador|silencioso|escucha|daemon|relay|prerrequisitos)' docs/verification/dispute-chat.md` returning ≥ 4.
5. **Anti-CRIT-1 reinforcement**:
   - `grep -cE 'CRIT-1' docs/verification/dispute-chat.md` ≥ 2.
   - `grep -cE '\.authors\(' docs/verification/dispute-chat.md` ≥ 2.
   - The bash grep one-liner with `^\s*[^/]` (excluding comments) is present verbatim.
6. **No emoji**: visual review of the file confirms zero emoji characters (the automated 4-byte UTF-8 grep is approximate; reviewer responsibility for the final pass).
7. **No code-leak**: `! grep -nE '^\s*//' docs/verification/dispute-chat.md` (no Rust-style code comments leaked into prose).
8. **No /api/notify smoke step**: `! grep -nE 'curl.*POST.*api/notify' docs/verification/dispute-chat.md` (this runbook covers the listener path only; `/api/notify` smoke lives in Plan 02 manual verification).
9. **Manual walkthrough (operator-side, before merging if requested)**: a Spanish-reading operator skims the four-section structure (Por qué / Prerrequisitos / Procedimiento / Limpieza) and confirms each step is executable as written (no missing context, no broken references, no dangling placeholders). Optional but recommended.

Commit message (literal — copy/paste, no Co-Authored-By trailer per project memory):

```
docs: add dispute chat verification runbook

Add docs/verification/dispute-chat.md — a Spanish operator runbook
that documents how to verify end-to-end that admin DMs (sent directly
user-to-user, NOT routed through the Mostro daemon) reach registered
devices as silent pushes via the existing Nostr-listener path.

The runbook reinforces the anti-CRIT-1 anti-fix at the operator level:
no .authors(mostro_pubkey) filter must ever be added to
src/nostr/listener.rs Filter::new() chain. Includes the bash grep
one-liner an operator runs after every deploy to confirm the
anti-fix has not been re-introduced.

This is the sole deliverable for VERIFY-03 in phase 02. No source code
changes; no test code; manual runbook only per phase 02 D-18. Phase 3
will introduce the in-process integration test harness for /api/notify
(VERIFY-01) but the dispute-chat path stays manual — Apple/FCM
delivery decisions cannot be unit-tested.
```
</verification>

<success_criteria>
- `docs/verification/dispute-chat.md` exists with at least 50 lines and ~600 words of substantive Spanish prose.
- The four sections from D-17 are present: registrar pubkey via `/api/register`, publicar `kind 1059` desde un segundo cliente Nostr (NOT Mostro daemon), verificar `info!("Push sent successfully for event ...")` + device receives push, anti-CRIT-1 grep check.
- The runbook references OOS-19 / CRIT-1 / VERIFY-03 / NIP-59 explicitly.
- The bash grep one-liner with `^\s*[^/]` is present verbatim.
- No emojis. No Rust code comments. No reference to `curl POST /api/notify` (that smoke is in Plan 02).
- Single file in `git diff --name-only HEAD`: `docs/verification/dispute-chat.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-post-api-notify-endpoint-with-privacy-hardening/02-03-SUMMARY.md` documenting:
- Files created (`docs/verification/dispute-chat.md`).
- Decisions applied (D-17, D-18).
- Commit SHA.
- Confirmation that the runbook is in Spanish per global CLAUDE.md and contains no emojis.
- Hand-off note for Phase 3: VERIFY-01 (in-process integration test harness) is the closest test analog of what this runbook documents manually for the dispute-chat path; the runbook is referenced from Phase 3's verification work as the "manual gold standard" the integration test must agree with.
</output>
